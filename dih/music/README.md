# Music beds

Drop royalty-free music tracks here (`.mp3 .wav .m4a .aac .flac .ogg`). One is
picked at random per video and ducked under the voiceover (target ~-22 dBFS,
set in config.json → music.vo_duck_db). Set music.enabled=false in config.json
to render VO-only.

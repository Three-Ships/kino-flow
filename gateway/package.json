{
  "name": "kino-flow-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Shared-key gateway for the Kino Flow distributed desktop app (Cloudflare Worker).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
